# visual-code-editor

Minimal package scaffold and shared workspace contract for dual-view code visualization.

This repository currently provides:

- a Python package scaffold under `src/visual_code_editor/`
- the shared `WorkspaceModel` dataclass contract
- placeholder ingest, render, asset, and validation modules
- baseline tests for partial organizational/behavioral population and cross-reference resolution
